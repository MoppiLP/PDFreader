export default {
  // Initial zoom level (1 = 100%)
  initialZoom: 1,
  
  // Zoom step for zoom in/out
  zoomStep: 0.1,
  
  // Maximum zoom level
  maxZoom: 3,
  
  // Minimum zoom level
  minZoom: 0.5
};

